/**
* The <<lumus>> program allows to manage the luminosity of the Linux 
* system (Use from the terminal) .
*
* Hash-package : md5sum(7c7655ce4b646d54f12e6e3b7dd8652a)
* Codename : electra
*
* Once the program is installed, except the `root`, only the users belonging to the
* group of this file `/sys/class/backlight/*/brightness` can execute properly 
* the `lumus` program .
*
* To see how to use <<lumus>> program, encode from the terminal (of 
* (course after installing the `lumus` program) `lumus --help` 
* or `man lumus` .
*/
