/**
* The <<lumus>> program allows to manage the luminosity of the Linux system(Use from the terminal) .
*
* Once the program is installed, except the `root`, only the users belonging to the
* group of this file `/sys/class/backlight/*/brightness` can execute properly 
* the `lumus` program .
*
* To see how to use <<lumus>> program, encode from the terminal(of course after installing the program) 
* `lumus --help` or `man lumus` .
*/
